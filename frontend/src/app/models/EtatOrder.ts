export enum EtatOrder {
    ACCEPTED = 'accepted',
    REFUSED = 'refused',
    WAITING = 'waiting',
    FINISHED = 'finished',
    REVIEWED = 'reviewed',
    CANCELED = 'canceled',
    DELIVRED = 'delivered',
}
